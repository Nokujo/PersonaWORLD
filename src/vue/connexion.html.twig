{% extends "base.html.twig" %}
{% block title %} {{ parent() }}Connexion{% endblock %}
{% block contenu %}

{% if form.valide is defined %}
    {% if form.valide == true %}
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">
                    <div class="card bg-dark text-light shadow-lg border-0 animate__animated animate__fadeIn">
                        <div class="card-body text-center py-5">
                            <h1 class="display-4 mb-4" style="color: var(--accent-color);">Connexion réussie</h1>

                            <p class="lead mb-2"> {% if session.role == 1 %}
                                Vous êtes connecté en tant qu’<strong>Administrateur</strong>.
                            {% else %}
                                Vous êtes connecté en tant que <strong>Client</strong>.
                            {% endif %} </p>
                            
                            <a href="index.php?page=accueil" class="btn btn-lg mt-4"
                               style="background: var(--primary-color); color: white; border-radius: 30px; font-weight: 450; padding: 0.75rem 2rem; box-shadow: var(--shadow); transition: transform 0.2s;">
                                ← Retour à l'accueil
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% else %}
        <div class="container mt-5 text-center">
            <div class="alert alert-danger shadow p-4">
                <h1 class="display-4">Échec de la connexion</h1>
                <p class="lead">Erreur : {{ form.message }}</p>
                <a href="index.php?page=connexion" class="btn btn-outline-danger mt-3">Réessayer</a>
            </div>
        </div>
    {% endif %}
{% else %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card bg-dark text-light shadow-lg">
                <div class="card-body">
                    <h2 class="text-center mb-4">🔐 Connexion à la Velvet Room</h2>
                    <p class="text-center text-muted mb-4">Identifiez-vous pour accéder à votre collection Persona</p>

                    <form method="post" action="index.php?page=connexion">
                        <div class="form-group">
                            <label for="inputEmail">Adresse Email</label>
                            <input type="email" class="form-control" id="inputEmail" name="inputEmail" placeholder="joker@phantom-thieves.com" required>
                        </div>

                        <div class="form-group">
                            <label for="inputPassword">Mot de passe</label>
                            <input type="password" class="form-control" id="inputPassword" name="inputPassword" placeholder="••••••••" required>
                        </div>

                        <button type="submit" name="btnConnexion" class="btn btn-primary btn-block mt-4">Se connecter</button>

                        <hr class="bg-secondary">

                        <p class="text-center mt-3">
                            Pas encore de compte ?
                            <a href="index.php?page=inscrire" class="text-info">Inscrivez-vous</a>
                        </p>
                        <p class="text-center mt-3">
                            <a href="index.php?page=accueil" class="btn btn-outline-light btn-sm">← Retour à l’accueil</a>
                        </p>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}
{% endblock %}
